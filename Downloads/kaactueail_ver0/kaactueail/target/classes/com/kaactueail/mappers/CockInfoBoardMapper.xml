<?xml version="1.0" encoding="UTF-8"?>
<!-- boardmapper.xml  DML 명령 처리 -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 	"http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
 	
 <!-- Mapper 인터페이스와 동일한 이름 주어야 하므로 패키지명+mapper이름으로 작명 --> 
 <!-- 쿼리문 id는 인터페이스 메소드와 동일하게 설정 --> 
  	
 <mapper namespace="com.kaactueail.mappers.CockInfoBoardMapper">
 	
 	<select id="selectAll"  resultType="com.kaactueail.dto.CockInfoBoardDTO">
 		select * from InfoBoard where infoBoardTitle 
 		in (select cImgName from Cockit)
 	</select>
   	<!-- 게시물 총 개수 -->
    <select id="getTotal" resultType="int">    
        select count(*) from InfoBoard    
    </select>
 	 
<!--  	 <insert id="insert"   parameterType="com.kaactueail.dto.CockInfoBoardDTO">
 	 	insert into infoboard(infoBoardNum,infoBoardTitle,infoBoardContent,infoBoardRecipe,
 	 	 infoBoardDate,infoBoardWriter,infoBoardReadcount,infoBoardAdminNum)
 	 	values(infoboardSeq.nextval,#{infoBoardTitle},#{infoBoardContent},
 	 	 #{infoBoardRecipe},sysdate,#{infoBoardWriter},#{infoBoardReadcount},#{infoBoardAdminNum})
 	 </insert> -->
 	  
 	 <insert id="write">
 	 	insert into infoboard(infoBoardNum,infoBoardTitle,infoBoardContent,infoBoardRecipe, infoBoardWriter,infoBoardReadcount,infoBoardDate,infoBoardAdminNum,infoboardImgname)
 	 	values(infoboardNumSeq.nextval,#{infoBoardTitle}, #{infoBoardContent}, #{infoBoardRecipe}, #{infoBoardWriter},#{infoBoardReadcount},sysdate,1,#{infoBoardTitle})
 	 </insert>
 	 <select id="getDetail"  resultType="com.kaactueail.dto.CockInfoBoardDTO">
 	 	select * from infoboard where infoBoardNum = #{infoBoardNum}
 	 </select>
 	 
 	 
 	<!--  <delete id="delete" parameterType="int">
 	 	DELETE FROM infoboard WHERE SEQ = #{SEQ}
 	 </delete>
 	 
 	 
 	  <update id="update" parameterType="dto">
    	UPDATE infoboard
    		SET title=#{title} , writer=#{writer} , content=#{content} 	
    		WHERE seq = #{seq}
    </update>
    
     <select id="selectSearch" parameterType="hashMap" resultType="dto">
    	 
    		 1. Java 코드
    		 Map<String, String> map = new HashMap<>(); //collection
      		 map.put("column",column ); //column : title or writer or content
      		 map.put("keyvalue", keyvalue); //keyvalue : 김연아
             
             2. 함수 호출시 
             session.selectList("GUEST.selectSearch" ,map);
    	
    	     3. xml에서 
    		 parameterType="hashMap"
    		
    		map key   : column , keyvalue
    		map value : #{column} , ${column}
    		
    		Tip : ${column} : 컬럼명
    	
    	
    	SELECT * FROM infoboard
    	where writer like '%김연아%'
    	<if test="column != null">
    		where ${column} like '%' || #{keyvalue} || '%'    	
    	</if>
    </select>
    
     <select id="selectSearch2" parameterType="hashMap" resultType="dto">
    	SELECT * FROM infoboard
    	<where>
    		<if test="name != null">
    			title = #{keyvalue}
    		</if>
    	    <if test="email != null">
    	    	or writer = #{keyvalue}
    	    </if>
			<if test="home != null">
				or content = #{keyvalue}
			</if>    	
    	</where>    	
    </select> -->
 	 
 </mapper>